已发现的问题：

1. deepseek 的用量和没有正确显示，这里我想你参考codexbar中的实现，实现deepseek的余额显示，这里的余额应该根据apikey获取。多个apikey应该可以同时显示。
2. 服务器配置面板，监听地址和监听端口。输入框的右侧仍然显示了标签，这个标签应该移除
3. codebuddy的AccessToken的输入框，部分，按钮“添加Token（公司/个人切换用）”，按钮需要居左放置，显示“添加 Token” 即可
4. Codebuddy的供应商，显示CodeBuddy cn，然后其模型列表是否能够做成动态，可参考omniroute。图标可以查找tencent的图标看是否存在
5. 网关密钥，选择模型，我希望不同的供应商，能够增加一个分割线隔开。
6. 供应商列表，Open AI图标使用Codex的，OpenCode Go 的图标，和OpenCode一致。然后我希望能讲供应商增加一个拖动排序，参考codexbar。
7. 模型映射，我希望模型名称能够默认附加前缀，比如deepseek-v4-flash，显示为cbcn/deepseek-v4-flash，其他供应商的模型也类似。要不然如果有相同模型名称的，会导致无法增加多个供应商的模型。
8. antigravity的配置完成，测试也能通过，模型连接也ok，但是接入ai中报错。我希望能在通用的设置中增加一个测试，这个测试面板可以根据网关密钥，选择模型后提供一个输入框，用于测试模型响应情况。要不然每次都需要配置完整流程后到agent中进行测试。

以上问题帮我检查进行修复。有疑问使用grill-me和我对齐需求。
