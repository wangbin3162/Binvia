# 优化

1. 网关密钥配置面板，选择模型时，能否按照不同供应商增加一个分组，可以展开收起的，现在供应商太多了滚动条太多不太方便选择
2. Antigravity 和Codex 供应商配置手动配置Token展开的时候，两个输入框显示没有间隔贴在一起了，而且不知道这两个token分别是什么。需要优化。
3. CodeBuddy 增加AccessToken的时候，我输入了标签，添加后，标签没有正确显示，还是显示了Token的缩写。
4. GUI主面板展开概况这里如果添加的供应商较多我希望能够自动增加高度，服务显示区域，我希望下方”已停止”状态能和右侧启动停止按钮放在一排。上方的服务端点显示，能够独占一行。这样不会折行，文字可以稍微增大一些。
5. 菜单栏的图标，有点小，稍微放大一点点。
6. GUI面板和打开设置的Binvia Settings的面板。背景稍微透明一些，统一颜色，这部分参考设置的侧边栏背景颜色。
