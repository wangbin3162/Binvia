# 主面板优化

主面板展开现在只能看到usage和服务开关，现在希望针对主面板进行优化，首页显示概况，并且提供服务开关和用量统计。这部分都参考codexbar。

采用tab方式展示已接入的供应商，展示余额或者用量。

---

界面优化：

1. 基础信息面板，我希望对齐codexbar。logo区域和启用禁用下方直接跟别名，base url等信息。然后“信息”二字可以不现实。
2. API令牌部分。标签和key输入。key前方有个文字标签，这个标签不需要显示，直接在第二个输入框中placehoder 中提示“粘贴API密钥/Token”
3. CodeBuddy cn 改名为CodeBuddy CN 。
4. 服务器设置中，监听地址其实不需要支持修改，默认localhost，然后监听端口帮我更改成20427，在“服务器”区域下方，增加一个“API 端点”显示区域。展示本地端点：http://localhost:20427/v1 并且提供一个复制图标可以复制。这个端点。

用量优化：

1. 目前z.ai和opencode还没有实现用量获取。这部分查看一下是否可以有方式显示余额
2. codebuddy cn的用量显示，是否可以参考antigravity方式，通过登陆网页来查看积分余额。

---
