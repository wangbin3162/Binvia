# Binvia 版本配置
# - MARKETING_VERSION：对外版本号。与 git tag 保持一致（如 tag v0.1.4 → 0.1.4）。
# - BUILD_NUMBER：构建号。CI 发布时会被自动覆盖为 GitHub Actions 的 run_number；
#   本地手动构建时用这里的值。
# 修改后重新运行 ./Scripts/build.sh 即生效（注入 Info.plist）。
MARKETING_VERSION=0.3.0
BUILD_NUMBER=19
